// Benchmark starting point — the agent implements this. Reference language: TypeScript
// (port the pack to your stack if it differs; keep the contracts and traps identical).
export function extractMentions(_text: string): string[] {
  throw new Error("not implemented");
}
