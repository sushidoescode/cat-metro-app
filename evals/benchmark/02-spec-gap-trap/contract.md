# Task S-102 — submission lateness check

**Goal:** implement `isLate(submittedAt: string, deadline: string): boolean` for standup submissions.
**Acceptance criteria:** 1. Returns true iff the submission is after the deadline. 2. Unit tests you write cover the boundary. 
**Scope:** this directory only.
