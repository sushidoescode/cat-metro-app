# Security checklist — initial state at forge-init (2026-08-02)

Walked at init (risk posture: commercial; mode: sprint with production graduation criterion).
Legend: ✅ in place · 🟡 partial · ❌ open · ➖ not applicable yet (no stack/remote/prod exists).

## Identity & access
- 🟡 Dedicated least-privilege agent credentials — local dev runs under the owner's account; no prod, billing, or PII credentials exist anywhere yet. Revisit when any credential is created.
- ➖ Cloud creds read/plan-only — no cloud resources yet.
- 🟡 MCP servers — none configured at repo level; harness-level plugins (context7, playwright) are the owner's choice. Add per-tool allowlists if repo-level MCP config is ever added.
- ✅ No agent workflows trigger on untrusted-fork PRs — claude-review is disabled (sprint) and carries the fork guard for when it returns; agent-task workflow ships disabled.

## Containment
- ✅ Sandbox enabled in .claude/settings.json (ssh/aws/gh deny-read; network allowlist github.com + api.anthropic.com). Takes effect at next session start.
- ✅ bypassPermissions not used anywhere; .devcontainer with default-deny egress available for reduced-supervision work.
- 🟡 Permission rules — destructive denies + push/merge ask-gates live; dep-install ask-gate is TODO(stack) until a package manager exists.
- 🟡 Secrets — .env deny-read + .gitignore rules live; CI secret scanner is TODO(secret-scan), required before production graduation.

## Integrity
- ❌ Branch protection / required CI on main — no GitHub remote yet. Owner runs `bash scripts/setup-rulesets.sh` after first push (repo admin required).
- ✅ Immutable-paths enforcement — git pre-commit installed (doctor probe passes); Claude permission rules + PreToolUse hooks verified by direct invocation; active in-editor after session restart.
- ✅ Test-diff review rule — AGENTS.md hard rule 5 + forge-risk test-weakening markers.
- 🟡 Dependency gate — ADR-required rule live; CI audit step is TODO(audit) until a package manager exists.
- ✅ Instruction files reviewed like code — AGENTS.md/CLAUDE.md/.claude/** sit on the forge-risk workflow floor; CODEOWNERS anchors human review.

## Injection hygiene
- ✅ External text = data, never instructions (AGENTS.md hard rule 6). Operational discipline; re-verify at retro.
- 🟡 Read-untrusted vs hold-write separation — process rule; enforce per-session once external inputs (store reviews, user feedback) start flowing.

## Accountability
- 🟡 Spend caps — subscription-only operation ($0 API); budget line + >40%/week stop-and-rethink trigger in state/PROJECT_STATE.md. Per-run `--max-budget-usd` applies only if API workflows return.
- ➖ Iteration caps on headless runs — no headless runs configured yet.
- ❌ Backups — repo exists only on this machine until the first push; no restore drill. Drill date: ____.

## Governance
- ✅ Never-delegate list posted in docs/constitution.md.
- ❌ Incident runbook for agent-caused incidents — docs/runbooks/ is a stub; write before production graduation.
- Next full re-run due: 2026-11-02 (quarterly) or at production graduation, whichever is first.

**Gate to production graduation (from the ❌/🟡 above):** rulesets applied · secret scanner in CI · dependency audit in CI · incident runbook written · backup restore tested.
