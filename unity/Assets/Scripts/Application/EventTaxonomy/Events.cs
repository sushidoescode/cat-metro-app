namespace CatMetro.Application.EventTaxonomy
{
    // CM-C9 criterion 2: the 45 typed factories — the ONLY `new AnalyticsEvent(` site in
    // shipped code lives behind these (criterion 7's grep). Factory names are the PascalCase
    // form of the CSV's snake_case event names, in bijection both directions. RED STUB.
    public static class Events
    {
    }
}
