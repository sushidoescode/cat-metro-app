<?xml version="1.0" encoding="utf-8"?>
<!-- CM-C2b criterion 11 — RK-17 decided by the human 2026-08-04: CLOUD BACKUP OFF.
     allowBackup="false" disables Android cloud backup, so no backup-rules XML exists. It is not
     an unconditional device-migration guarantee: Android 12+ documents that some manufacturers'
     device-to-device transfers ignore this flag. The merged-manifest proof (gradle output) rides
     criterion 8's human device build. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          android:installLocation="preferExternal">
    <supports-screens
        android:smallScreens="true" android:normalScreens="true"
        android:largeScreens="true" android:xlargeScreens="true"
        android:anyDensity="true" />
    <application android:allowBackup="false"
                 android:label="@string/app_name"
                 android:icon="@mipmap/app_icon" />
</manifest>
