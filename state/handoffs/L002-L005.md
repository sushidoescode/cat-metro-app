# MINI-CONTRACT — L002–L005 onboarding authoring (P7; written FIRST per the phase-6-10 handoff)

No frozen backlog contract exists for this phase; this note IS the contract, frozen at branch
time on `task/L002-L005-onboarding`. Review verifies against it. Law: band table
(product_spec §21 as encoded in `CorpusValidator.BandTable`, LOCKED) · CM-R13 no-text tutorial
trio (L001–L003) · CM-R06.2 one-new-mechanic-at-a-time · schema v2 (FROZEN for the window) ·
AMD-09 (`validatedAt` never authored) · importer pins (single source, no wild — NEW-Q35/Q-B).

## Goal

Author `content/levels/L002.json` … `L005.json` so the campaign corpus grows 1/30 → 5/30:
the tutorial trio completes (L002–L003, switch-only), `queue` enters at L004 as the declared
`newMechanic` (schema enum `queue`; an authored `queueCapacity` node becomes meaningful), and
L005 combines switch+queue at the top of the onboarding difficulty ramp. Greybox-validated is
the bar — taste/fun stays human (stage 11 PENDING is expected and non-blocking).

## Acceptance criteria (per level, via `bash scripts/validate-content.sh`)

1. **Blocking stages green:** Schema (1) · StaticAnalysis (2; decoy-station WARNs allowed) ·
   Solver (4) verdict **Solved** — BFS-exact, ≤ 2 switches used · TrivialityReject (5): the
   zero-input run must NOT win (pin or timeout both acceptable) · Brittleness (6): jitter
   retention ≥ 70% over unpinned samples AND every solver-log entry's action window ≥ the
   level's `minActionWindowTicks`.
2. **Band law:** band `onboarding`; `difficultyTarget` ascending from L001's 0.08 within
   [0.05, 0.16]: L002 0.10 · L003 0.12 · L004 0.14 · L005 0.16. Campaign band-table assertion
   green.
3. **Mechanic order (CM-R06.2, campaign assertion green):** L002/L003 `mechanics ["switch"]`,
   `newMechanic null` (nothing new — re-teaching); L004 `mechanics ["switch","queue"]`,
   `newMechanic "queue"`; L005 `mechanics ["switch","queue"]`, `newMechanic null`.
4. **`minActionWindowTicks` ∈ 12–16** (onboarding band law), tightening as difficulty rises:
   16 · 14 · 13 · 12.
5. **Solver-optimal seconds printed** in the report; the 40–75 s comparison stays
   `PINNED(NEW-Q1)` — printed, never compared, never blocking.
6. **`meta.validatedAt` ABSENT** in every authored file (AMD-09; stamping is Q-O/human-CI).
7. **`authoredBy": "llm+validator"`** (schema enum — the honest provenance row).
8. **Campaign corpus assertions green:** count 5/30 → PENDING (non-blocking, expected);
   mechanic order + band table PASS; L001 untouched (byte-identical — `git diff` empty on it).
9. **Whole-gate evidence:** `bash scripts/validate-content.sh` exit 0 over the full corpus;
   `bash scripts/check.sh` OK; `bash scripts/test.sh` all-green (5/5 on this branch base; the daily wrapper joins when CM-C6 #14 merges).

## Method

Author JSON → `bash scripts/validate-content.sh` → read the stage table → fix → repeat.
The solver is ground truth for solvability and action windows; no schema change, no Domain
edit, no `evals/` or `tests/contract/` touch. Board designs stay inside shipped mechanics
(single source, colors red/blue only, no wild, every multi-exit junction switched).

## Stop conditions

Schema change temptation → stop (frozen window). Domain edit → stop (golden-invalidating).
Second source / wild need → stop (pins). A level that cannot pass brittleness without
narrowing a window below 12 → redesign the board, never the band row. Taste judgments beyond
greybox → human (TG gates, stage 11).
