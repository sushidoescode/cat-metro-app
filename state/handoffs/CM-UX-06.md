# CM-UX-06 — adoption handoff note (UX lane, 2026-08-06)

## Provenance (cross-lane adoption, precedent-applied)

Built by the DEVICE session (contract ef70e72 → red efae0a7 → green 9fadb9a → evidence 9b24cba,
anchored at ca13801). Adopted by the UX lane per the CM-UX-04 adoption precedent (human ruling
2026-08-06) and the human's "then CM-UX-06" directive; the precedent application is stated, not
assumed silently — the human can override. Adoption branch `task/CM-UX-06-adopted`.

## Contract

`state/handoffs/CM-UX-06-frozen-contract.md` (their freeze, first-commit-anchored). The lane
never drafted a competing contract for this slice — no comparison verdict needed.

## What the adoption added

1. **The third append-append resolution** (the now-standing merge-order law): their csv rows
   7-9 shift to 9-11 behind main's hint.tutorial/results.next; `UiCsvDisciplineTests` composes
   to count 5+7; their `UiCsvUx06Tests` absolute pins AND count assert shifted with in-test
   documentation. csv = 12 rows, every one pinned.
2. **This handoff note** (absent from their branch — the recurring cross-lane gap; their
   evidence frames were present this time, a first).
3. Session eyeballs on all three of their frames: Home motion-off shows the title, three
   UNMARKED parked-district silhouettes (S-05's curiosity-not-locks law), and the L001 pin's
   cream raised-ring shape state — **A11Y-S01-2's motion-off twin demonstrated in a still**;
   LevelIntro shows name + substituted goal + the explicit thumb-band Play CTA (the #27 review's
   demand honored). Session-1 structural law visually confirmed on top of their tree test.

## Evidence

Six-slice combined tree (rings + chip + panel + stack/Home/LevelIntro over main @ 7176cf9):
**EditMode 745/745 · PlayMode 88/88** (their slice adds 15 EM + 10 PM; every count delta
reconciles). Full harness on the committed tree: recorded in the PR (13 wrappers, re-derived per the standing law — #42 review F12; and the
contract wording precision: criteria byte-frozen + status log append-only, not byte-unchanged).

## Forward obligations (the CM-UX-07 ledger, final form)

`BoardInputActive` bind · `MotionOffSource` bind + Retry() rebind · `ScreenChromeController`
attach · the human-approved halt escape · `OnDisable` unregister law (R2-3) · F4 world-corners
read-backs · F5 TMP proxy upgrade · F11 vocabulary-guard enrolment decision · the Home/stack
mount behind a launch argument (boot stays L001 — human Q-5 default) · the zero-text inventory amendment for home.title/intro.play/intro.goal (#42 review F4 — the LegalTexts list must gain these BEFORE any flow renders them during Playing) · the batched TG sitting
(three DRAFT strings + home.title/intro.play/intro.goal now added, the tinted ring, chip
placement, results-panel weight, the F13 footer inversion, the two-text-stacks interim).
